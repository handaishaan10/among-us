<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Scan QR</title>
    <link rel="stylesheet" href="/style.css">
    <audio id="killSound" src="/kill.mp3" preload="auto"></audio>
</head>
<body>
    <div class="container">
        <h1>🔍 Scan QR</h1>
        <form action="/game/scan" method="POST">
            <input type="text" name="qrCodeID" placeholder="Enter QR Code ID" required>
            <button type="submit">Kill</button>
        </form>

        <% if (result) { %>
            <div class="result"><%= result %></div>

            <% if (result.includes("has been killed")) { %>
                <script>
                    const sound = document.getElementById("killSound");
                    sound.volume = 0.7;
                    sound.play();
                </script>
            <% } %>
        <% } %>

        <a href="/game/dashboard">⬅ Back to Dashboard</a>
    </div>

    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #111;
            color: white;
            text-align: center;
            padding: 2rem;
        }

        .container {
            background-color: #222;
            padding: 2rem;
            border-radius: 10px;
            display: inline-block;
        }

        input, button {
            padding: 0.7rem;
            margin: 0.5rem;
            border-radius: 5px;
            border: none;
        }

        input {
            width: 250px;
        }

        button {
            background-color: #ff4747;
            color: white;
            font-weight: bold;
            cursor: pointer;
        }

        .result {
            margin-top: 1rem;
            font-size: 1.2rem;
        }

        a {
            display: block;
            margin-top: 2rem;
            color: #77ddff;
        }
    </style>
</body>
</html>
